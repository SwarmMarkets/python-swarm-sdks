"""Web3 constants for Swarm Collection."""

from ..models import Network

# ERC20 ABI (minimal for common operations)
ERC20_ABI = [
    {
        "constant": True,
        "inputs": [{"name": "_owner", "type": "address"}],
        "name": "balanceOf",
        "outputs": [{"name": "balance", "type": "uint256"}],
        "type": "function",
    },
    {
        "constant": False,
        "inputs": [
            {"name": "_to", "type": "address"},
            {"name": "_value", "type": "uint256"},
        ],
        "name": "transfer",
        "outputs": [{"name": "", "type": "bool"}],
        "type": "function",
    },
    {
        "constant": False,
        "inputs": [
            {"name": "_spender", "type": "address"},
            {"name": "_value", "type": "uint256"},
        ],
        "name": "approve",
        "outputs": [{"name": "", "type": "bool"}],
        "type": "function",
    },
    {
        "constant": True,
        "inputs": [
            {"name": "_owner", "type": "address"},
            {"name": "_spender", "type": "address"},
        ],
        "name": "allowance",
        "outputs": [{"name": "remaining", "type": "uint256"}],
        "type": "function",
    },
    {
        "constant": True,
        "inputs": [],
        "name": "decimals",
        "outputs": [{"name": "", "type": "uint8"}],
        "type": "function",
    },
    {
        "constant": True,
        "inputs": [],
        "name": "symbol",
        "outputs": [{"name": "", "type": "string"}],
        "type": "function",
    },
    {
        "constant": True,
        "inputs": [],
        "name": "name",
        "outputs": [{"name": "", "type": "string"}],
        "type": "function",
    },
]

# RPC endpoints for different networks (public RPCs)
RPC_ENDPOINTS = {
    Network.ETHEREUM: "https://eth.llamarpc.com",
    Network.POLYGON: "https://polygon-rpc.com",
    Network.BASE: "https://mainnet.base.org",
}

# Networks that require PoA middleware
POA_NETWORKS = [Network.POLYGON]

# Gas buffer multiplier (adds 20% to gas estimates for safety)
GAS_BUFFER_MULTIPLIER = 1.2

# Default gas limit for ERC20 transfers (fallback if estimation fails)
DEFAULT_GAS_LIMIT = 100000

# Transaction timeout in seconds
TX_TIMEOUT = 300
